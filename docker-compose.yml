version: "3.8"

services:
  telegram-bot:
    build: .
    container_name: telegram-bot-tool
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data          # 持久化数据库和备份文件
    # 如果你想自定义时区，可以取消下面注释
    # environment:
    #   - TZ=Asia/Shanghai